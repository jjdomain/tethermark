name: security

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  static-security:
    runs-on: ubuntu-latest
    steps:
      - uses: github/codeql-action/analyze@0123456789abcdef0123456789abcdef01234567
      - name: security-summary
        run: echo "security scan marker"
