# Architecture

This fixture exists to test review handling of ambiguous repository content.

Notes:

- secrets in `tests/fixtures` are synthetic
- eval and benchmark terms are included intentionally
- reviewers should distinguish fixture noise from deployable risk
