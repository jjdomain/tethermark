# Runtime Node Smoke Fixture

This dependency-free fixture is used to prove that a real audit target can run tests and start a bounded service inside the Local Runtime Sandbox. It must never execute directly on the host.
